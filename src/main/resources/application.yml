server:
  port: 8080

spring:
  application:
    name: ManagementPerpustakaan

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/db_perpustakaan
    username: root
    password: rizal280697
